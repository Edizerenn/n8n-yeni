# n8n'in en son resmi Docker imajını kullan
FROM n8nio/n8n

# n8n için varsayılan portu ayarla
ENV N8N_PORT=5678
EXPOSE 5678

# (Opsiyonel) Eğer özelleştirmeleriniz varsa buraya ekleyebilirsiniz.
# Örneğin, özel bir düğüm eklemek isterseniz:
# RUN npm install -g n8n-nodes-mycustomnode

# Eğer başka özel dosyalarınız varsa kopyalayın
# COPY package.json .
# COPY .n8n .n8n

# n8n'i başlat
CMD ["n8n", "start"]
